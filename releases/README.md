# 发布目录

此目录用于保留可直接下载的、已验证的分发版本，而非构建过程中的临时输出。

- `windows/x86_64/`：Windows x64 安装包。
- 未来可增加 `windows/arm64/`、`linux/x86_64/`、`macos/aarch64/` 等目录。

日常构建输出仍位于根目录 `dist/`，并由 Git 忽略；只有准备对外分发的版本才复制到本目录。正式发布时应同时上传同一文件到 GitHub Release，并在 Release 说明中记录版本号、变更摘要和 SHA-256。
