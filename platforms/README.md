# 平台适配预留

目前的产品实现和安装包只支持 Windows。此目录的作用是隔离各操作系统与桌面环境相关的代码、安装脚本和发布说明。

- `windows/`：资源管理器右键菜单、Win32 分层窗口、UAC 与 Inno Setup。
- `linux/`：未来的文件管理器扩展、Wayland/X11 透明层、桌面包格式。
- `macos/`：未来的 Finder 扩展、透明窗口、权限与 `.dmg`/`.pkg` 打包。

跨平台功能应共享动画状态、素材调度和删除意图；每个平台继续使用其原生的权限和文件回收站接口。
